# wget

> این دستور یک نام مستعار از `wget -p common` است.
> اطلاعات بیشتر: <https://learn.microsoft.com/powershell/module/microsoft.powershell.utility/invoke-webrequest>.

- مشاهده مستندات دستور اصلی :

`tldr wget -p common`
