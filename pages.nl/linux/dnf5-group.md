# dnf5 group

> Dit commando is een alias van `dnf group`.
> Opmerking: van Fedora 37 tot 40 (inclusief) voert `dnf` DNF v4 uit, terwijl `dnf5` DNF v5 uitvoert.

- Bekijk de documentatie van het originele commando:

`tldr dnf group`
