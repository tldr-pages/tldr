# docker container rm

> Tämä on alias komennolle `docker rm`.
> Lisätietoa: <https://docs.docker.com/reference/cli/docker/container/rm/>.

- Näytä alkuperäisen komennon dokumentaatio:

`tldr docker rm`
