# systemd-confext

> এই কমান্ডটি `systemd-sysext` - এর একটি উপনাম।
> এটি `systemd-sysext`-এর মতো একই নীতি অনুসরণ করে, তবে `/usr` এবং `/opt`-এ কাজ করার পরিবর্তে `confext` শুধুমাত্র `/etc`-কে এক্সটেন্ড করে।

- মূল কমান্ডের ডকুমেন্টেশন দেখুন:

`tldr systemd-sysext`
