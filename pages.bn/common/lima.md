# lima

> এই কমান্ডটি ডিফল্ট VM ইনস্ট্যান্সের জন্য `limactl shell` এর একটি উপনাম।
> আপনি অন্য একটি ইনস্ট্যান্সে কাজ করতে `$LIMA_INSTANCE` এনভারমেন্ট ভেরিয়েবল সেট করতে পারেন।

- মূল কমান্ডের ডকুমেন্টেশন দেখুন:

`tldr limactl`
