# git whatchanged

> এই কমান্ডটি `git log --raw --no-merges` এর একটি উপনাম (প্রতিটি কমিটের জন্য raw diffs সহ লগ)।
> নোট: এই কমান্ডটি deprecated এবং ভবিষ্যতে সরানো হবে।
> আরও তথ্য পাবেন: <https://git-scm.com/docs/git-whatchanged>।

- মূল কমান্ডের ডকুমেন্টেশন দেখুন:

`tldr git log`
