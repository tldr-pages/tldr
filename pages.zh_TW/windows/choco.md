# choco

> 「Chocolatey package manager」軟體套件管理器。
> 執行命令由兩個單字「組合」成，例如 `choco install`，請參考使用文檔。
> 更多資訊：<https://chocolatey.org>.

- 執行「組合」命令：

`choco {{命令}}`

- 顯示 `choco` 幫助文檔：

`choco -?`

- 顯示「組合」命令幫助文檔：

`choco {{命令}} -?`

- 顯示 Chocolatey 版本：

`choco --version`
