# cd

> 切換目前的工作目錄。
> 更多資訊：<https://www.gnu.org/software/bash/manual/bash.html#index-cd>.

- 切換為指定目錄：

`cd {{目錄/完整/路徑}}`

- 切換為目前目錄的上層目錄：

`cd ..`

- 切換為目前使用者的主目錄：

`cd`

- 切換為指定使用者的主目錄：

`cd ~{{使用者名稱}}`

- 切換為之前所在的目錄：

`cd -`

- 切換為根目錄：

`cd /`
