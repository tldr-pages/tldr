# |

> Խողովակային տվյալները ծրագրերի միջև:.
> Լրացուցիչ տեղեկություններ. <https://gnu.org/software/bash/manual/bash.html#Pipelines>:.

- Խողովակ `stdout` դեպի `stdin`:

`{{command}} | {{command}}`

- Խողովակ՝ `stdout` և `stderr`-ին `stdin`:

`{{command}} |& {{command}}`
