# freeramdisk

> Ազատ հիշողություն, որն օգտագործվում է `loadlin` ramdisk-ի կողմից հին համակարգերում:.
> Հիմնականում հնացած է `umount`, `losetup` և `tmpfs` կողմից:.
> Լրացուցիչ տեղեկություններ. <https://manned.org/freeramdisk>:.

- Անվճար `loadlin` ramdisk հիշողություն.:

`sudo freeramdisk`
