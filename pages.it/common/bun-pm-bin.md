# bun pm bin

> Mostra il percorso delle directory `bin`.
> Maggiori informazioni: <https://bun.com/docs/pm/cli/pm#bin>.

- Ottieni il percorso della directory `bin` del progetto locale:

`bun pm bin`

- Ottieni il percorso della directory `bin` globale:

`bun pm bin {{[-g|--global]}}`
