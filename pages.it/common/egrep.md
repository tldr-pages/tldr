# egrep

> Questo comando è un alias per `grep --extended-regexp`.
> Vedi anche: `regex`.
> Maggiori informazioni: <https://manned.org/egrep>.

- Consulta la documentazione del comando originale:

`tldr grep`
