# pkgctl auth

> Autentica `pkgctl` con servizi come GitLab.
> Maggiori informazioni: <https://manned.org/pkgctl-auth>.

- Autentica `pkgctl` con l'istanza GitLab:

`pkgctl auth login`

- Mostra lo stato di autenticazione:

`pkgctl auth status`
