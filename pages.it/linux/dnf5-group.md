# dnf5 group

> Questo comando è un alias per `dnf group`.
> Nota: Da Fedora 37 a 40 (inclusi), `dnf` esegue DNF v4 mentre `dnf5` esegue DNF v5.

- Consulta la documentazione del comando originale:

`tldr dnf group`
