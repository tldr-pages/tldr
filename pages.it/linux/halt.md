# halt

> Arresta, spegne o riavvia la macchina.
> Maggiori informazioni: <https://manned.org/halt>.

- Arresta la macchina:

`halt`

- Spegne la macchina:

`halt {{[-p|--poweroff]}}`

- Riavvia la macchina:

`halt --reboot`
