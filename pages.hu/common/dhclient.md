# dhclient

> DHCP-ügyfél. További információ: <https://manned.org/dhclient>.

- IP-cím megszerzése a `eth0` interfészhez:

`sudo dhclient {{eth0}}`

- IP-cím kiadása a `eth0` interfészhez:

`sudo dhclient -r {{eth0}}`
