# git credential-store

> `git` segédprogram a jelszavak lemezen való tárolásához. További információ: <https://git-scm.com/docs/git-credential-store>.

- Git hitelesítő adatok tárolása egy adott fájlban:

`git config credential.helper 'store --file={{path/to/file}}'`
