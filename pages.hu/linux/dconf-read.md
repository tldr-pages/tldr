# dconf read

> Kulcsértékek olvasása a dconf adatbázisokból. Lásd még: `dconf`. További információ: <https://manned.org/dconf>.

- Egy adott kulcsérték kinyomtatása:

`dconf read {{/path/to/key}}`

- Egy adott kulcs \[d\]efault értékének nyomtatása:

`dconf read -d {{/path/to/key}}`
