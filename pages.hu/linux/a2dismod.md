# a2dismod

> Egy Apache modul letiltása Debian-alapú operációs rendszereken. További információ: <https://manpages.debian.org/latest/apache2/a2dismod.8.en.html>.

- Egy modul letiltása:

`sudo a2dismod {{module}}`

- Ne jelenítsen meg tájékoztató üzeneteket:

`sudo a2dismod --quiet {{module}}`
