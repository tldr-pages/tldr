# automountd

> 用于 `autofs` 的自动挂载/卸载守护进程。按需由 `launchd` 启动。
> 不应该手动调用。
> 更多信息：<https://keith.github.io/xcode-man-pages/automountd.8.html>.

- 启动守护进程：

`automountd`

- 将更多详细信息记录到 `syslog`：

`automountd -v`
