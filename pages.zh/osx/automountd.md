# automountd

> 一个用于 `autofs` 的自动挂载/卸载守护进程，由 `launchd` 按需启动。
> 不应手动调用。
> 更多信息：<https://keith.github.io/xcode-man-pages/automountd.8.html>。

- 启动守护进程：

`automountd`

- 向 `syslog` 记录更多详细信息：

`automountd -v`
