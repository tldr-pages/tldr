# pkgin

> 管理 NetBSD 上的 `pkgsrc` 二进制软件包。
> 更多信息：<https://pkgin.net/#usage>.

- 安装软件包：

`pkgin install {{package}}`

- 卸载软件包及其依赖项：

`pkgin remove {{package}}`

- 升级所有软件包：

`pkgin full-upgrade`

- 搜索软件包：

`pkgin search {{keyword}}`

- 列出已安装的软件包：

`pkgin list`

- 移除不必要的依赖项：

`pkgin autoremove`
