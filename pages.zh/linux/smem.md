# smem

> 打印程序的内存使用情况。
> 更多信息：<https://manned.org/smem>.

- 打印当前进程的内存使用情况：

`smem`

- 打印系统上每个用户的当前进程内存使用情况：

`smem --users`

- 打印指定用户的当前进程内存使用情况：

`smem --userfilter {{username}}`

- 打印系统内存信息：

`smem --system`