# kmod

> 管理 Linux 内核模块。
> 该程序通常通过其符号链接调用：`lsmod`、`rmmod`、`insmod`、`modinfo`、`modprobe` 和 `depmod`。
> 更多信息：<https://manned.org/kmod>。

- 列出当前已加载的内核模块：

`kmod list`

- 显示当前运行内核模块提供的静态设备节点信息：

`kmod static-nodes`
