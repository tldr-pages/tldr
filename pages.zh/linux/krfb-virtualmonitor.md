# krfb-virtualmonitor

> 创建一个虚拟显示器，允许该显示器通过 VNC 使用。
> 更多信息：<https://invent.kde.org/network/krfb>.

- 创建一个虚拟显示器：

`krfb-virtualmonitor --resolution {{1920}}x{{1080}} --name {{monitor_name}} --password {{password}} --port {{5900}}`
