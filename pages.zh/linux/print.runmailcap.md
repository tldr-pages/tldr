# print

> `run-mailcap` 的 `print` 动作别名。
> `run-mailcap` 最初用于处理 MIME 类型/文件。
> 更多信息：<https://manned.org/print>.

- 默认的 `run-mailcap` 工具可以使用 `print` 动作打印任何文件：

`print {{filename}}`

- 使用 `run-mailcap`：

`run-mailcap --action=print {{filename}}`
