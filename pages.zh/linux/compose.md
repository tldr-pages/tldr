# compose

> `run-mailcap` 的 `compose` 动作别名。
> 原本 `run-mailcap` 用于处理 mime-type/文件。
> 更多信息：<https://manned.org/compose>.

- `compose` 动作可用于使用默认的邮件编辑工具创建或编辑任何现有文件或新文件：

`compose {{filename}}`

- 与 `run-mailcap` 一起使用：

`run-mailcap --action=compose {{filename}}`
