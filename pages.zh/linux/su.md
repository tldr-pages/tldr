# su

> 切换 shell 到另一个用户。
> 更多信息：<https://manned.org/su>.

- 切换到超级用户（需要root密码）：

`su`

- 切换到指定用户（需要输入该用户的密码）：

`su {{用户名}}`

- 切换到指定用户并模拟一个完整的登录 shell ：

`su - {{用户名}}`

- 以另一个用户身份执行命令:

`su - {{用户名}} {{[-c|--command]}} "{{命令}}"`
