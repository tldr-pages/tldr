# cgroups

> cgroups，也称为控制组，是 Linux 内核的一个特性，用于限制、测量和控制进程的资源使用。
> 不过，cgroups 本身并不是一个命令，而是一系列命令的集合，参见以下相关页面。
> 更多信息：<https://www.kernel.org/doc/Documentation/cgroup-v2.txt>.

- 查看 `cgclassify` 的文档：

`tldr cgclassify`

- 查看 `cgcreate` 的文档：

`tldr cgcreate`

- 查看 `cgexec` 的文档：

`tldr cgexec`
