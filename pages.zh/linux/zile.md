# zile

> 一个轻量级的 Emacs 文本编辑器克隆。
> 更多信息：<https://www.gnu.org/software/zile/>.

- 启动一个用于临时笔记的缓冲区，不会保存：

`zile`

- 打开一个文件：

`zile {{path/to/file}}`

- 保存文件：

`<Ctrl x><Ctrl s>`

- 退出：

`<Ctrl x><Ctrl c>`

- 在指定行号处打开文件：

`zile +{{line_number}} {{path/to/file}}`

- 撤销更改：

`<Ctrl x><u>`