# mesg

> 检查或设置终端接收来自其他用户的消息的能力，通常是通过 `write` 命令。
> 另请参见 `write`，`talk`。
> 更多信息：<https://manned.org/mesg>.

- 检查终端是否允许接收消息：

`mesg`

- 禁止接收来自其他用户的消息：

`mesg n`

- 允许接收来自其他用户的消息：

`mesg y`

- 启用详细模式，如果命令不是从终端执行，则打印警告：

`mesg {{[-v|--verbose]}}`
