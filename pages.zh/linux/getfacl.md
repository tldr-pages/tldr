# getfacl

> 获取文件访问控制列表（ACL）。
> 更多信息：<https://manned.org/getfacl>。

- 显示文件访问控制列表：

`getfacl {{path/to/file_or_directory}}`

- 显示带有数字用户和组ID的文件访问控制列表：

`getfacl {{[-n|--numeric]}} {{path/to/file_or_directory}}`

- 以表格格式显示文件访问控制列表：

`getfacl {{[-t|--tabular]}} {{path/to/file_or_directory}}`
