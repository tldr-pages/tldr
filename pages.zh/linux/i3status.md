# i3status

> 用于 i3 窗口管理器的状态栏。
> 该命令通常在 i3 配置文件中调用。
> 更多信息：<https://i3wm.org/i3status/manpage.html>。

- 使用默认配置定期打印状态栏到 `stdout`：

`i3status`

- 使用特定配置定期打印状态栏到 `stdout`：

`i3status -c {{path/to/i3status.conf}}`

- 显示帮助和版本信息：

`i3status -h`
