# paxs

> 跨 Yay、Flatpak 和 Snap 管理软件包。
> 支持搜索、安装、卸载和升级软件包。
> 更多信息：<https://github.com/zamhedonia/paxs>.

- 搜索软件包：

`paxs {{search_term}}`

- 升级所有软件包：

`paxs -u`

- 安装软件包（提示选择来源）：

`paxs -i {{package}}`

- 卸载软件包（提示选择来源）：

`paxs -r {{package}}`

- 检查所有软件包管理器的更新：

`paxs -c`

- 显示帮助：

`paxs -h`
