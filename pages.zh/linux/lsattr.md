# lsattr

> 列出 Linux 系统下的文件属性。
> 更多信息：<https://manned.org/lsattr>.

- 显示当前目录下文件的属性：

`lsattr`

- 列出指定路径下的文件属性：

`lsattr {{path}}`

- 递归列出当前目录及其子目录中所有文件属性：

`lsattr -R`

- 显示当前目录下所有文件的属性，包括隐藏文件：

`lsattr -a`

- 显示当前目录下的目录属性：

`lsattr -d`
