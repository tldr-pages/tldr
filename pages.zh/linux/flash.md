# flash

> 在终端中使用闪卡。
> 更多信息：<https://github.com/tallguyjenks/fla.sh>.

- 打开可用闪卡组的选择菜单：

`flash`

- 显示闪卡系统的相关信息：

`flash -i`

- 将预览器从默认的 `bat` 更改为 `cat`：

`flash -p {{cat}}`

- 显示帮助信息：

`flash -h`

- 显示版本信息：

`flash -v`