# abrt-action-analyze-c

> 为包含 `coredump` 的问题数据目录计算 UUID。
> 更多信息：<https://manned.org/abrt-action-analyze-c>。

- 为当前工作目录计算并保存 UUID：

`abrt-action-analyze-c`

- 为特定目录计算并保存 UUID：

`abrt-action-analyze-c -d {{path/to/directory}}`

- 详细计算并保存 UUID：

`abrt-action-analyze-c -v`
