# hwclock

> 读取或更改硬件时钟。通常需要 root 权限。
> 更多信息：<https://manned.org/hwclock>.

- 显示硬件时钟报告的当前时间：

`hwclock`

- 将当前的系统时间写入硬件时钟（有时在系统设置期间使用）：

`hwclock {{[-w|--systohc]}}`

- 将当前的硬件时间写入系统时间：

`hwclock {{[-s|--hctosys]}}`
