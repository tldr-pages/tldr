# ifmetric

> 一个用于操纵 IPv4 路由度量的工具。
> 更多信息：<https://0pointer.de/lennart/projects/ifmetric/>.

- 设置指定网络接口的优先级（数字越大表示优先级越低）：

`sudo ifmetric {{interface}} {{value}}`

- 重置指定网络接口的优先级：

`sudo ifmetric {{interface}} {{0}}`
