# iftop

> 显示接口按主机的带宽使用情况。
> 更多信息：<https://manned.org/iftop>。

- 显示带宽使用情况：

`sudo iftop`

- 显示指定接口的带宽使用情况：

`sudo iftop -i {{interface}}`

- 显示带有端口信息的带宽使用情况：

`sudo iftop -P`

- 不显示流量的条形图：

`sudo iftop -b`

- 不查询主机名：

`sudo iftop -n`

- 显示帮助：

`<?>`