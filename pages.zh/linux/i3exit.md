# i3exit

> 退出 i3 窗口管理器。
> 更多信息：<https://gitlab.manjaro.org/packages/community/i3/i3exit>。

- 注销 i3：

`i3exit logout`

- 锁定 i3：

`i3exit lock`

- 关机：

`i3exit shutdown`

- 挂起系统：

`i3exit suspend`

- 切换到登录屏幕，以不同用户身份登录：

`i3exit switch_user`

- 休眠系统：

`i3exit hibernate`

- 重启系统：

`i3exit reboot`
