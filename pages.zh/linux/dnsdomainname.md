# dnsdomainname

> 显示系统的 DNS 域名。
> 注意：该工具使用 `gethostname` 获取系统的主机名，然后使用 `getaddrinfo` 将其解析为规范名称。
> 更多信息：<https://www.gnu.org/software/inetutils/manual/html_node/dnsdomainname-invocation.html>。

- 显示系统的 DNS 域名：

`dnsdomainname`