# edit

> `run-mailcap` 的编辑操作别名。
> `run-mailcap` 用于处理/编辑 MIME 类型/文件。
> 更多信息：<https://www.computerhope.com/unix/uedit.htm>。

- 使用默认的 mailcap 浏览器查看任何文件：

`edit {{filename}}`

- 使用 `run-mailcap`：

`run-mailcap --action=edit {{filename}}`
