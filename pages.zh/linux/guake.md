# guake

> GNOME 的下拉式终端。
> 更多信息：<https://github.com/Guake/guake>。

- 切换 Guake 的可见性：

`<F12>`

- 切换全屏模式：

`<F11>`

- 打开一个新标签页：

`<Ctrl Shift t>`

- 关闭终端：

`<Super x>`

- 切换到上一个标签页：

`<Ctrl PageUp>`

- 在浏览器中搜索选中的文本：

`<Ctrl Shift l>`
