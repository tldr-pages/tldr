# nm-online

> 询问 NetworkManager 网络是否已连接。
> 更多信息：<https://networkmanager.pages.freedesktop.org/NetworkManager/NetworkManager/nm-online.html>.

- 检查网络是否已连接并将结果打印到 `stdout`：

`nm-online`

- 等待 `n` 秒建立连接（默认为 30 秒）：

`nm-online --timeout {{n}}`
