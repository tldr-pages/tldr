# dunstctl

> `dunst` 的控制命令。
> 更多信息：<https://manned.org/dunstctl>.

- 暂停通知：

`dunstctl set-paused true`

- 取消暂停通知：

`dunstctl set-paused false`

- 关闭所有通知：

`dunstctl close-all`

- 显示帮助：

`dunstctl --help`
