# mke2fs

> 在分区中创建 Linux 文件系统。
> 更多信息：<https://manned.org/mke2fs>。

- 在设备 b 的分区 1 (`sdb1`) 中创建 ext2 文件系统：

`mkfs.ext2 {{/dev/sdb1}}`

- 在设备 b 的分区 1 (`sdb1`) 中创建 ext3 文件系统：

`mkfs.ext3 {{/dev/sdb1}}`

- 在设备 b 的分区 1 (`sdb1`) 中创建 ext4 文件系统：

`mkfs.ext4 {{/dev/sdb1}}`