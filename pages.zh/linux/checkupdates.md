# checkupdates

> 检查 Arch Linux 中待更新的软件包。
> 更多信息：<https://manned.org/checkupdates.8>.

- 列出待更新的软件包：

`checkupdates`

- 列出待更新的软件包并将其下载到 `pacman` 缓存中：

`checkupdates --download`

- 使用特定的 `pacman` 数据库列出待更新的软件包：

`CHECKUPDATES_DB={{path/to/directory}} checkupdates`

- 显示帮助：

`checkupdates --help`
