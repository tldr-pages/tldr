# terminator

> 在一个窗口中排列多个 GNOME 终端。
> 更多信息：<https://gnome-terminator.org/>.

- 启动 `terminator` 窗口：

`terminator`

- 以全屏方式启动：

`terminator -f`

- 水平分割终端：

`<Ctrl Shift o>`

- 垂直分割终端：

`<Ctrl Shift e>`

- 打开新标签页：

`<Ctrl Shift t>`