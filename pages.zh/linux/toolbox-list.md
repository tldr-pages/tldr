# toolbox list

> 列出现有的 `toolbox` 容器和镜像。
> 更多信息：<https://manned.org/toolbox-list.1>.

- 列出所有的 `toolbox` 容器和镜像：

`toolbox list`

- 仅列出 `toolbox` 容器：

`toolbox list --containers`

- 仅列出 `toolbox` 镜像：

`toolbox list --images`