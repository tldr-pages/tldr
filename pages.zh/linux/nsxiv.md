# nsxiv

> Neo Simple X 图像查看器。
> 更多信息：<https://nsxiv.codeberg.page/man>.

- 打开图像：

`nsxiv {{path/to/file1 path/to/file2 ...}}`

- 以图像模式从目录中打开图像：

`nsxiv {{path/to/directory1 path/to/directory2 ...}}`

- 递归搜索目录中的图像以查看：

`nsxiv -r {{path/to/directory1 path/to/directory2 ...}}`

- 退出 nsxiv：

`<q>`

- 切换到缩略图模式或在图像模式下打开选定的图像：

`<Enter>`

- 在图像模式下向前浏览图像：

`<n>`

- 在图像模式下向后浏览图像：

`<p>`