# yakuake

> KDE 的下拉式终端。
> 更多信息：<https://manned.org/yakuake>.

- 启动 Yakuake:

`yakuake`

- 切换 Yakuake 的显示状态:

`<F12>`

- 切换全屏模式:

`<F11>`

- 打开新标签页:

`<Ctrl Shift t>`

- 在标签页之间切换:

`<Shift {{ArrowLeft|ArrowRight}}>`

- 创建终端分割:

`<Ctrl {{)|(}}>`

- 在分割之间切换:

`{{<Ctrl Tab>|<Ctrl Shift Tab>}}`