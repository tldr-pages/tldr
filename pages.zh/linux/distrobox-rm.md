# distrobox-rm

> 移除 Distrobox 容器。更多信息：[distrobox-rm 使用说明](https://distrobox.it/usage/distrobox-rm)。另见：`tldr distrobox`。

- 移除 Distrobox 容器（提示：在移除前先停止容器）：

`distrobox-rm {{container_name}}`

- 强制移除 Distrobox 容器：

`distrobox-rm {{container_name}} --force`
