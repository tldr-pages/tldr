# toolbox

> 管理 Linux 上的容器化命令行环境。
> 一些子命令（如 `create`）有自己的使用文档。
> 更多信息：<https://manned.org/toolbox.1>.

- 运行 `toolbox` 子命令：

`toolbox {{subcommand}}`

- 显示特定子命令的帮助（如 `create`、`enter`、`rm`、`rmi` 等）：

`toolbox help {{subcommand}}`

- 显示帮助：

`toolbox --help`

- 显示版本：

`toolbox --version`