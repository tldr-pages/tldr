# hardinfo

> 在 GUI 窗口中显示硬件信息。
> 更多信息：<https://github.com/lpereira/hardinfo>.

- 启动 `hardinfo`：

`hardinfo`

- 将报告输出到 `stdout`：

`hardinfo -r`

- 将报告保存为 HTML 文件：

`hardinfo -r -f html > hardinfo.html`