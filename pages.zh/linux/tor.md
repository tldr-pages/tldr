# tor

> 通过 Tor 网络启用匿名通信。
> 更多信息：<https://manned.org/tor>。

- 连接至 Tor 网络：

`tor`

- 查看 Tor 配置：

`tor --config`

- 检查 Tor 状态：

`tor --status`

- 仅作为客户端运行：

`tor --client`

- 作为中继运行：

`tor --relay`

- 作为桥接运行：

`tor --bridge`

- 作为隐藏服务运行：

`tor --hidden-service`