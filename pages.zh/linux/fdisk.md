# fdisk

> 管理硬盘上的分区表和分区。
> 参见: `partprobe`。
> 更多信息: <https://manned.org/fdisk>。

- 列出分区：

`sudo fdisk -l`

- 启动分区管理器：

`sudo fdisk {{/dev/sdX}}`

- 在分区硬盘时，创建一个新分区：

`<n>`

- 在分区硬盘时，选择要删除的分区：

`<d>`

- 在分区硬盘时，查看分区表：

`<p>`

- 在分区硬盘时，保存所做的更改：

`<w>`

- 在分区硬盘时，放弃所做的更改：

`<q>`

- 在分区硬盘时，打开帮助菜单：

`<m>`
