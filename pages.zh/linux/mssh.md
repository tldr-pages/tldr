# mssh

> 用于同时与多个 SSH 服务器交互的基于 GTK+ 的 SSH 客户端。
> 更多信息：<https://manned.org/mssh>.

- 打开一个新窗口并连接到多个 SSH 服务器：

`mssh {{user@host1}} {{user@host2}} {{...}}`

- 打开一个新窗口并连接到在 `~/.mssh_clusters` 中预定义的服务器组：

`mssh --alias {{alias_name}}`