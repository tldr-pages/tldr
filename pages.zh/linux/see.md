# see

> `run-mailcap` 的 `view` 动作的别名。
> `run-mailcap` 的 `print` 动作的别名。
> 更多信息：<https://manned.org/see>.

- 使用 see 动作可以使用默认的 mailcap 浏览器查看任何文件（通常是图像）：

`see {{filename}}`

- 与 `run-mailcap` 一起使用：

`run-mailcap --action=view {{filename}}`
