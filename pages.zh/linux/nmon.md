# nmon

> 系统管理员、调优工具和基准测试工具。
> 更多信息：<https://manned.org/nmon>。

- 启动 `nmon`：

`nmon`

- 将记录保存到文件（默认为 "-s 300 -c 288"）：

`nmon -f`

- 将记录保存到文件，总共进行 240 次测量，每次测量间隔 30 秒：

`nmon -f -s {{30}} -c {{240}}`
