# dir

> 使用每行一个文件的格式列出目录内容，特殊字符使用反斜杠转义序列表示。功能等同于 ls -C --escape。
> 更多信息：<https://manned.org/dir>.

- 列出所有文件，包括隐藏文件：

`dir {{[-a|--all]}}`

- 列出文件并显示其作者（需要 -l 参数）：

`dir -l --author`

- 列出文件，排除匹配指定通配符模式的文件：

`dir --hide {{模式}}`

- 递归列出子目录：

`dir {{[-R|--recursive]}}`

- 显示帮助信息：

`dir --help`
