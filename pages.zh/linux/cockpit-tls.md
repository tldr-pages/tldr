# cockpit-tls

> 终止 TLS 的 HTTP 代理，用于加密客户端与 `cockpit-ws` 之间的流量。
> 更多信息：<https://cockpit-project.org/guide/latest/cockpit-tls.8.html>。

- 为特定端口（而非默认端口 `9090`）提供 HTTP 请求服务：

`cockpit-tls --port {{port}}`

- 显示帮助：

`cockpit-tls --help`
