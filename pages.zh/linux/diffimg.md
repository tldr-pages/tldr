# diffimg

> 计算两张图像之间的交集。
> 注意：支持的扩展名有 `.png`、`.gif`、`.jpg`、`.ps`。
> 更多信息：<https://manned.org/diffimg>。

- 计算两张图像之间的差异，并输出一个图像，其中每个像素都是输入图像中对应像素的差异：

`diffimg {{path/to/input_image1.ext}} {{path/to/input_image2.ext}} {{path/to/output_image.ext}}`