# locale

> 获取特定于语言环境的信息。
> 更多信息：<https://manned.org/locale>.

- 列出描述用户语言环境的所有全局环境变量：

`locale`

- 列出所有可用的语言环境：

`locale {{[-a|--all-locales]}}`

- 显示所有可用的语言环境及其关联的元数据：

`locale {{[-a|--all-locales]}} {{[-v|--verbose]}}`

- 显示当前的日期格式：

`locale date_fmt`