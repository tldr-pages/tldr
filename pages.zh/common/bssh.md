# bssh

> 一个用于浏览本地网络上的 SSH/VNC 服务器的图形工具。
> 另见：`bvnc` 和 `bshell`。
> 更多信息：<https://manned.org/bssh>。

- 浏览 SSH 服务器：

`bssh`

- 浏览 VNC 服务器：

`bssh --vnc`

- 浏览 SSH 和 VNC 服务器：

`bssh --shell`

- 在指定域中浏览 SSH 服务器：

`bssh --domain {{domain}}`