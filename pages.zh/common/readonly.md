# readonly

> 设置只读 shell 变量。
> 更多信息：<https://www.gnu.org/software/bash/manual/bash.html#index-readonly>。

- 设置只读变量：

`readonly {{变量名}}={{值}}`

- 将变量标记为只读：

`readonly {{现有变量}}`

- 将所有只读变量的名称和值打印到 `stdout`：

`readonly -p`
