# php yii

> Yii 框架的命令行接口。
> 更多信息：<https://yiiframework.com>.

- 启动当前 Yii 应用的 PHP 内置 web 服务器：

`php yii {{serve}}`

- 为指定的模型类生成控制器、视图和相关文件，以实现在该模型上执行 CRUD 操作：

`php yii {{gii/crud}} --modelClass={{ModelName}} --controllerClass={{ControllerName}}`

- 显示帮助：

`php yii {{help}}`