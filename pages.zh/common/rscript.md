# Rscript

> 使用 R 编程语言运行脚本。
> 更多信息：<https://www.r-project.org>.

- 运行脚本：

`Rscript {{path/to/file.R}}`

- 以纯模式运行脚本（即不保存工作区的空白会话）：

`Rscript --vanilla {{path/to/file.R}}`

- 执行一个或多个 R 表达式：

`Rscript -e {{expression1}} -e {{expression2}}`

- 显示 R 版本：

`Rscript --version`
