# git pr

> 本地检出 GitHub 拉取请求。
> 属于 `git-extras` 部分。
> 更多信息： <https://github.com/tj/git-extras/blob/master/Commands.md#git-pr>.

- 检出特定的拉取请求：

`git pr {{pr_number}}`

- 从特定的远程仓库检出拉取请求：

`git pr {{pr_number}} {{remote}}`

- 从拉取请求的 URL 检出：

`git pr {{url}}`

- 清理旧的拉取请求分支：

`git pr clean`
