# seq

> 打印数字序列到标准输出。
> 更多信息：<https://www.gnu.org/software/coreutils/seq>.

- 从 1 到 10 的序列：

`seq 10`

- 从5开始，3为步长，不超过20的序列：

`seq 5 3 20`

- 从5开始，3为步长，不超过20的序列，并用空格作为分隔符：

`seq -s " " 5 3 20`

- 从5开始，3为步长，不超过20的序列，并格式化为4位宽度，不足4位，前面补0：

`seq -f "%04g" 5 3 20`
