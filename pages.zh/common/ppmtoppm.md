# ppmtoppm

> 复制 PPM 图像。
> 更多信息：<https://netpbm.sourceforge.net/doc/ppmtoppm.html>。

- 从 `stdin` 复制一个 PPM 图像（即 PBM、PGM 或 PPM 图像）到 `stdout`：

`ppmtoppm < {{path/to/image.ppm}} > {{path/to/output.ppm}}`

- 显示版本：

`ppmtoppm -version`