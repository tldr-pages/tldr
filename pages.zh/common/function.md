# function

> 定义函数。
> 更多信息：<https://www.gnu.org/software/bash/manual/bash.html#Shell-Functions>。

- 定义一个具有指定名称的函数：

`function {{func_name}} { {{echo "函数内容"}}; }`

- 运行名为 `func_name` 的函数：

`func_name`

- 显示帮助：

`help function`