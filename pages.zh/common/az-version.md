# az version

> 显示 Azure CLI 模块和扩展的当前版本。
> 作为 `azure-cli`（也称为 `az`）的一部分。
> 更多信息：<https://learn.microsoft.com/cli/azure/reference-index?view=azure-cli-latest#az-version>。

- 以 JSON 格式显示 Azure CLI 模块和扩展的当前版本：

`az version`

- 以指定格式显示 Azure CLI 模块和扩展的当前版本：

`az version --output {{json|table|tsv}}`