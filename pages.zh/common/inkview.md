# inkview

> Inkscape 图形 SVG 预览器。
> 也可以作为幻灯片查看器。
> 更多信息：<https://wiki.inkscape.org/wiki/index.php/Inkview>.

- 预览一个 SVG：

`inkview {{path/to/file.svg}}`

- 预览多个 SVG（使用 `<ArrowKeys>` 导航）：

`inkview {{path/to/file1.svg path/to/file2.svg ...}}`