# git unlock

> 解锁 Git 仓库中的文件，使其可以被提交修改。
> 这是 `git-extras` 的一部分。另见 `git lock`。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-unlock>.

- 解锁以前锁定的本地文件，以便提交其更改：

`git unlock {{path/to/file}}`
