# bshell

> 用于在局域网中浏览 SSH/VNC 服务器的图形界面工具。
> 详见：`bssh` 和 `bvnc`。
> 更多信息：<https://manned.org/bshell>。

- 浏览 SSH 和 VNC 服务器：

`bshell`

- 仅浏览 SSH 服务器：

`bshell --ssh`

- 仅浏览 VNC 服务器：

`bshell --vnc`

- 在指定域中浏览 SSH 和 VNC 服务器：

`bshell --domain {{domain}}`