# paci

> Bash 脚本的包管理器。
> 更多信息：<https://github.com/tradebyte/paci>。

- 更新可用包及其版本的列表（建议在执行其他 `paci` 命令之前运行此命令）：

`paci refresh`

- 配置其行为：

`paci configure`

- 搜索指定的包：

`paci search {{package}}`

- 安装一个包：

`paci install {{package}}`

- 更新一个包：

`paci update {{package}}`
