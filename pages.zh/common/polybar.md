# polybar

> 一个快速且易用的状态栏。
> 更多信息：<https://github.com/polybar/polybar/wiki>.

- 启动 Polybar（如果配置中只有一个状态栏，则可以省略状态栏名称）：

`polybar {{bar_name}}`

- 使用指定的配置启动 Polybar：

`polybar --config={{path/to/config.ini}} {{bar_name}}`

- 启动 Polybar 并在配置文件修改时重新加载状态栏：

`polybar --reload {{bar_name}}`
