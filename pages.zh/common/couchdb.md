# couchdb

> Apache CouchDB 数据库服务器的命令行接口。
> 更多信息：<https://couchdb.apache.org>.

- 启动 CouchDB：

`couchdb`

- 启动 CouchDB 交互式 Shell：

`couchdb -i`

- 作为后台进程启动 CouchDB：

`couchdb -b`

- 终止后台进程（注意：如果需要，它会重新启动）：

`couchdb -k`

- 关闭后台进程：

`couchdb -d`