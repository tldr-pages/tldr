# ClamAV

> 开源反病毒程序。
> ClamAV 并不是一个单一的命令，而是一组命令。
> 更多信息：<https://www.clamav.net>。

- 查看使用 `clamd` 守护进程扫描文件的文档：

`tldr clamdscan`

- 查看在没有 `clamd` 守护进程运行时扫描文件的文档：

`tldr clamscan`

- 查看更新病毒定义的文档：

`tldr freshclam`
