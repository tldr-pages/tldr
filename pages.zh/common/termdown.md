# termdown

> 命令行下的倒计时计时器和秒表。
> 更多信息：<https://github.com/trehn/termdown>.

- 启动秒表：

`termdown`

- 启动1分钟30秒的倒计时：

`termdown {{1m30s}}`

- 启动1分钟30秒的倒计时，并在结束时使终端闪烁：

`termdown {{1m30s}} --blink`

- 在倒计时上方显示标题：

`termdown {{1m30s}} --title "{{有意义的标题}}"`

- 显示当前时间：

`termdown --time`
