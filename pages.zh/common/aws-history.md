# aws history

> 打印 AWS CLI 命令的命令行历史记录（必须已启用 AWS CLI 命令历史记录）。
> 更多信息：<https://docs.aws.amazon.com/cli/latest/reference/history/>.

- 列出命令历史并显示命令 ID：

`aws history list`

- 根据给定的命令 ID 显示与特定命令相关的事件：

`aws history show {{命令_id}}`
