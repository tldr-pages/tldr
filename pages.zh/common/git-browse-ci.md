# git browse-ci

> 在默认的网页浏览器中打开当前 `git` 仓库的 CI 网站。
> 属于 `git-extras` 的一部分。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-browse-ci>。

- 在上游网站上打开当前仓库的 CI 配置：

`git browse-ci`

- 在上游网站上打开指定远程仓库的 CI 配置：

`git browse-ci {{remote}}`
