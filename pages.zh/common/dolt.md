# dolt

> 一个可以像 Git 仓库一样 fork、clone、branch、merge、push 和 pull 的 SQL 数据库。
> 一些子命令如 `commit` 有自己的使用文档。
> 更多信息：<https://docs.dolthub.com/cli-reference/cli>.

- 执行一个 `dolt` 子命令：

`dolt {{subcommand}}`

- 列出可用的子命令：

`dolt help`