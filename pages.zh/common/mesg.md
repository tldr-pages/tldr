# mesg

> 检查或设置终端是否可以接收来自其他用户的消息，通常来自 `write` 命令。
> 也可参见 `write`, `talk`。
> 更多信息：<https://manned.org/mesg.1p>.

- 检查终端是否允许接收消息：

`mesg`

- 禁止接收来自 `write` 命令的消息：

`mesg n`

- 允许接收来自 `write` 命令的消息：

`mesg y`
