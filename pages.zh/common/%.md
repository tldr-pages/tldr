# %

> 管理作业。
> 更多信息：<https://www.gnu.org/software/bash/manual/bash.html#Job-Control-Basics>.

- 将当前作业切换到前台：

`%`

- 将上一个作业切换到前台：

`%-`

- 将 `n` 号作业切换到前台：

`%{{n}}`

- 将命令以 `字符串` 开头的作业切换到前台：

`%{{字符串}}`

- 将命令包含 `字符串` 的作业切换到前台：

`%?{{字符串}}`

- 恢复一个已暂停的作业：

`%{{1}} &`
