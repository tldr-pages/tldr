# sniffer.py

> 使用原始套接字捕获并显示指定协议的网络数据包。
> 是 Impacket 套件的一部分。
> 更多信息：<https://github.com/fortra/impacket>.

- 捕获默认协议（ICMP、TCP、UDP）的数据包：

`sniffer.py`

- 捕获特定协议（例如，ICMP、TCP）的数据包：

`sniffer.py {{protocol1}} {{protocol2}}`

- 捕获特定协议（例如，TCP）的数据包：

`sniffer.py tcp`