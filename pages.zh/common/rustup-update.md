# rustup update

> 更新 Rust 工具链和 `rustup` 本身（如果未使用包管理器安装）。
> 更多信息：<https://rust-lang.github.io/rustup>.

- 更新所有已安装的工具链和 `rustup`：

`rustup update`

- 安装或更新特定的工具链（更多信息请参阅 `rustup help toolchain`）：

`rustup update {{toolchain}}`