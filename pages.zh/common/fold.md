# fold

> 将输入文件的每行换行以适合指定宽度，并打印到 `stdout`。
> 更多信息：<https://manned.org/fold.1p>。

- 将每行换行到默认宽度（80 个字符）：

`fold {{路径/到/文件}}`

- 将每行换行到宽度 30：

`fold -w30 {{路径/到/文件}}`

- 将每行换行到宽度 5，并在空格处断行（将每个空格分隔的单词放在新行中，长度 > 5 的单词会被换行）：

`fold -w5 -s {{路径/到/文件}}`
