# npm init

> 创建一个 `package.json` 文件。
> 更多信息：<https://docs.npmjs.com/cli/commands/npm-init>。

- 通过提示初始化新包：

`npm init`

- 使用默认值初始化新包：

`npm init -y`

- 使用特定的初始化程序初始化新包：

`npm init {{create-react-app}} {{my-app}}`