# brew outdated

> 列出过期的酒杯（casks）和配方（formulae）。
> 要升级所有项目，请使用 `brew upgrade`。
> 更多信息：<https://docs.brew.sh/Manpage#outdated-options-formulacask->。

- 列出所有过期的酒杯和配方：

`brew outdated`

- 仅列出过期的配方：

`brew outdated --formula`

- 仅列出过期的酒杯：

`brew outdated --cask`
