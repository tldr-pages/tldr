# enable

> 启用和禁用 shell 内建命令。
> 更多信息：<https://www.gnu.org/software/bash/manual/bash.html#index-enable>。

- 打印内建命令列表：

`enable`

- 禁用内建命令（仅在 Bash 中有效）：

`enable -n {{命令}}`

- 重新启用内建命令：

`enable {{命令}}`
