# rtv

> Reddit 终端查看器。
> 使用 `<ArrowKeys>` 导航。使用 `<ArrowRight>` 和 `<ArrowLeft>` 查看和返回提交。
> 更多信息：<https://github.com/michael-lazar/rtv>。

- 打开首页：

`/front`

- 打开子版块：

`/r/{{subreddit_name}}`

- 展开/折叠评论：

`<Space>`

- 打开链接：

`<o>`

- 登录：

`<u>`

- 显示帮助：

`<?>`