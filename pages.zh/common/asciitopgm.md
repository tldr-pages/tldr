# asciitopgm

> 将 ASCII 图形转换为 PGM 文件。
> 更多信息：<https://netpbm.sourceforge.net/doc/asciitopgm.html>。

- 读取 ASCII 数据作为输入，并生成一个 PGM 图像，该图像的像素值是 ASCII 字符“亮度”的近似值：

`asciitopgm {{path/to/input_file}} > {{path/to/output_file.pgm}}`

- 显示版本：

`asciitopgm -version`