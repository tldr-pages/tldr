# git clear

> 清理 Git 工作目录，使其就像使用当前分支刚克隆的一样，包括 `.gitignore` 中包含的文件。
> `git-extras` 的一部分。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-clear>.

- 重置所有已跟踪的文件并删除所有未跟踪的文件，即使这些文件包含在 `.gitignore` 中：

`git clear`