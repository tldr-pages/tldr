# travis

> Travis CI 的命令行客户端。
> 更多信息：<https://github.com/travis-ci/travis.rb>.

- 显示客户端版本：

`travis version`

- 使用认证令牌对 CLI 客户端进行认证：

`travis login`

- 列出用户有权限访问的仓库：

`travis repos`

- 加密 `.travis.yml` 中的值：

`travis encrypt {{token}}`

- 生成一个 `.travis.yml` 文件并启用项目：

`travis init`