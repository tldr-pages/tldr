# git rm

> 从仓库索引和本地文件系统中删除文件。
> 更多信息：<https://git-scm.com/docs/git-rm>.

- 从仓库索引和文件系统中删除文件：

`git rm {{路径/到/文件}}`

- 删除目录：

`git rm -r {{路径/到/目录}}`

- 从仓库索引中删除文件，但保留本地文件：

`git rm --cached {{路径/到/文件}}`
