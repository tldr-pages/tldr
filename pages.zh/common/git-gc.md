# git gc

> 优化本地仓库，清理不必要的文件。
> 更多信息：<https://git-scm.com/docs/git-gc>。

- 优化仓库：

`git gc`

- 强烈优化，需要更多时间：

`git gc --aggressive`

- 不删除松散对象（默认删除）：

`git gc --no-prune`

- 抑制所有输出：

`git gc --quiet`

- 显示帮助：

`git gc --help`