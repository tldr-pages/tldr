# busybox

> 一个包含多种小型系统工具的单一可执行文件。
> 通过符号链接执行 `busybox` 等同于运行 `busybox symlink_name`。
> 使用 BusyBox 的 Linux 发行版通常会为所有程序提供符号链接。
> 更多信息：<https://www.busybox.net/downloads/BusyBox.html>。

- 执行一个 BusyBox 功能：

`busybox {{ls|rm|mkdir|cat|...}} {{args}}`

- 显示帮助信息和功能列表：

`busybox --help`
