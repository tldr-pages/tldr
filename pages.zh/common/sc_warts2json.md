# sc_warts2json

> 将 `warts` 文件中的信息导出为 JSON 格式。
> 更多信息：<https://www.caida.org/catalog/software/scamper/>。

- 将 `warts` 文件转换为 JSON 格式并输出结果：

`sc_warts2json {{path/to/file1.warts path/to/file2.warts ...}}`