# npm name

> 检查在 npm 上是否可以使用某个包或组织名称。
> 更多信息：<https://github.com/sindresorhus/npm-name-cli>.

- 检查特定的包名称是否在 `npm` 注册表中可用：

`npm-name {{package}}`

- 在 `npm` 注册表中查找类似的包名称：

`npm-name --similar {{package}}`
