# aws s3 mb

> 创建 S3 存储桶。
> 更多信息：<https://docs.aws.amazon.com/cli/latest/reference/s3/mb.html>.

- 创建一个 S3 存储桶：

`aws s3 mb s3://{{存储桶名称}}`

- 在指定区域中创建一个 S3 存储桶：

`aws s3 mb s3://{{存储桶名称}} --region {{区域}}`

- 显示帮助信息：

`aws s3 mb help`
