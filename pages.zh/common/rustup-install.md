# rustup install

> 安装或更新 Rust 工具链。
> 此命令是 `rustup update` 的别名，但一次只能安装或更新一个工具链。
> 更多信息：<https://rust-lang.github.io/rustup>.

- 安装或更新特定的工具链（更多信息见 `rustup help toolchain`）：

`rustup install {{toolchain}}`