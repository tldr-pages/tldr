# factor

> 打印一个数字的质因数分解。
> 更多信息：<https://www.gnu.org/software/coreutils/manual/html_node/factor-invocation.html>。

- 显示一个数字的质因数分解：

`factor {{number}}`

- 如果未指定参数，则从 `stdin` 获取输入：

`echo {{number}} | factor`
