# git info

> 显示 Git 仓库信息。
> 是 `git-extras` 的一部分。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-info>。

- 显示远程地址、远程和本地分支、最近的提交数据和 `.git/config` 设置：

`git info`

- 显示远程地址、远程和本地分支以及最近的提交数据：

`git info --no-config`
