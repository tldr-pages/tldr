# puppet

> 帮助管理和自动化服务器的配置。
> 一些子命令（如 `agent`）有自己独立的使用文档。
> 更多信息：<https://puppet.com/>。

- 执行 Puppet 子命令：

`puppet {{subcommand}}`

- 检查 Puppet 版本：

`puppet --version`

- 显示帮助信息：

`puppet --help`

- 显示子命令的帮助信息：

`puppet help {{subcommand}}`