# git show-ref

> 显示 Git 仓库中的引用列表。
> 更多信息：<https://git-scm.com/docs/git-show-ref>。

- 显示仓库中的所有引用：

`git show-ref`

- 只显示头部引用：

`git show-ref --heads`

- 只显示标签引用：

`git show-ref --tags`

- 验证给定的引用是否存在：

`git show-ref --verify {{path/to/ref}}`
