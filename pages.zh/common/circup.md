# circup

> CircuitPython 库更新器。
> 更多信息：<https://github.com/adafruit/circup>。

- 交互式更新设备上的模块：

`circup update`

- 安装新库：

`circup install {{library_name}}`

- 搜索库：

`circup show {{partial_name}}`

- 以 `requirements.txt` 格式列出连接设备上的所有库：

`circup freeze`

- 将连接设备上的所有库保存到当前目录：

`circup freeze -r`