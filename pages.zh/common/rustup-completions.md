# rustup completions

> 为 `rustup` 和 `cargo` 生成 shell 补全脚本。
> 更多信息：<https://rust-lang.github.io/rustup>.

- 将补全脚本打印到 `stdout`：

`rustup completions {{bash|elvish|fish|powershell|zsh}} {{rustup|cargo}}`
