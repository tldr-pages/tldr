# git mr

> 本地签出 GitLab 合并请求。
> 属于 `git-extras`。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-mr>。

- 签出特定的合并请求：

`git mr {{mr_number}}`

- 从特定的远程仓库签出合并请求：

`git mr {{mr_number}} {{remote}}`

- 从 URL 签出合并请求：

`git mr {{url}}`

- 清理旧的合并请求分支：

`git mr clean`
