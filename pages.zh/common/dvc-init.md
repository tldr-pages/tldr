# dvc init

> 初始化新的本地 DVC 仓库。
> 更多信息：<https://dvc.org/doc/command-reference/init>。

- 初始化新的本地仓库：

`dvc init`

- 不使用 Git 初始化 DVC：

`dvc init --no-scm`

- 在子目录中初始化 DVC：

`cd {{path/to/subdir}} && dvc init --subdir`