# mpd

> 音乐播放器守护进程。
> 参见：`mpc`, `ncmpcpp`。
> 更多信息：<https://www.musicpd.org/>。

- 启动 MPD：

`mpd`

- 启动 MPD 但不读取配置文件：

`mpd --no-config`

- 启动 MPD 并不将其从控制台分离：

`mpd --no-daemon`

- 结束当前正在运行的 MPD 会话：

`mpd --kill`
