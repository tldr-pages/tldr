# dvc

> 数据版本控制：类似于 `git` 的数据管理工具。
> 某些子命令（如 `commit`）有自己的使用文档。
> 更多信息：<https://dvc.org/>.

- 执行 DVC 子命令：

`dvc {{subcommand}}`

- 显示通用帮助信息：

`dvc --help`

- 显示特定子命令的帮助信息：

`dvc {{subcommand}} --help`

- 显示版本：

`dvc --version`
