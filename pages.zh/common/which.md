# which

> 在用户的 `$PATH` 中定位一个程序。
> 另请参阅：`whereis`、`type`。
> 更多信息：<https://manned.org/which>。

- 搜索 `$PATH` 环境变量并显示所有匹配可执行文件的位置：

`which {{可执行文件}}`

- 如果存在多个匹配的可执行文件，显示所有位置：

`which {{[-a|--all]}} {{可执行文件}}`
