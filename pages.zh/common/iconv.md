# iconv

> 将文本从一种编码转换为另一种编码。
> 更多信息：<https://manned.org/iconv>。

- 将文件转换为特定编码，并输出到 `stdout`：

`iconv {{[-f|--from-code]}} {{源编码}} {{[-t|--to-code]}} {{目标编码}} {{输入文件}}`

- 将文件转换为当前区域设置的编码，并输出到文件：

`iconv {{[-f|--from-code]}} {{源编码}} {{输入文件}} > {{输出文件}}`

- 列出支持的编码：

`iconv {{[-l|--list]}}`
