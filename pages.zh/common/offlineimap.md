# offlineimap

> 同步远程 IMAP 服务器和本地 Maildir 文件夹。
> 更多信息：<https://www.offlineimap.org>.

- 仅同步一次，不启用自动刷新：

`offlineimap -o`

- 同步特定账户：

`offlineimap -a {{account}}`

- 同步特定文件夹：

`offlineimap -f {{folder}}`