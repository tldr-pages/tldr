# pio

> 用于嵌入式板的开发环境。
> 某些子命令（如 `run`）有自己的使用文档。
> 更多信息：<https://docs.platformio.org/en/latest/core/userguide/>.

- 显示帮助信息并列出子命令：

`pio --help`

- 显示特定子命令的帮助信息：

`pio {{subcommand}} --help`

- 显示版本信息：

`pio --version`
