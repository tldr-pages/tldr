# tt

> 基于终端的打字测试工具。
> 更多信息：<https://github.com/lemnos/tt>。

- 使用内置的英文名言列表启动名言模式：

`tt -quotes {{en}}`

- 生成一个由50个随机单词组成的测试，分为5组，每组10个单词：

`tt -n {{10}} -g {{5}}`

- 启动一个持续10秒的计时测试：

`tt -t {{10}}`

- 启动 `tt`，不使用主题，并在打字时显示你的WPM（每分钟打字数）：

`tt -showwpm -notheme`
