# lastcomm

> 显示最后执行的命令。
> 更多信息：<https://manned.org/lastcomm>。

- 打印 acct（记录文件）中所有命令的信息：

`lastcomm`

- 显示特定用户执行的命令：

`lastcomm --user {{用户}}`

- 显示系统上执行的特定命令的信息：

`lastcomm --command {{命令}}`

- 显示在特定终端上执行的命令的信息：

`lastcomm --tty {{终端名称}}`
