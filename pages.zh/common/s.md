# s

> 从终端进行网络搜索。
> 更多信息：<https://github.com/zquestz/s>.

- 在 Google（默认搜索引擎）上搜索查询：

`s {{query}}`

- 列出所有搜索引擎：

`s --list-providers`

- 使用指定的搜索引擎进行查询搜索：

`s --provider {{provider}} {{query}}`

- 使用指定的二进制文件执行查询搜索：

`s --binary "{{binary}} {{arguments}}" {{query}}`
