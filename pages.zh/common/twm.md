# twm

> X Window 系统的窗口管理器。
> 更多信息：<https://gitlab.freedesktop.org/xorg/app/twm>。

- 连接到默认的 X 服务器：

`twm`

- 连接到特定的 X 服务器：

`twm -display {{display}}`

- 仅管理默认屏幕：

`twm -s`

- 使用特定的启动文件：

`twm -f {{path/to/file}}`

- 启用详细模式并在 X 中打印意外错误：

`twm -v`
