# more

> 交互式地显示文件，允许滚动和搜索。
> 请参阅：`less`.
> 更多信息：<https://manned.org/more.1p>.

- 打开一个文件：

`more {{路径/到/文件}}`

- 打开一个文件，从特定行开始显示：

`more +{{行号}} {{路径/到/文件}}`

- 转到下一页：

`<空格>`

- 搜索一个字符串（按 `<n>` 键跳转到下一个匹配）:

`</>{{字符串}}<Enter>`

- 退出：

`<q>`

- 显示关于交互式命令的帮助：

`<h>`
