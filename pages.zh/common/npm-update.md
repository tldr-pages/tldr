# npm update

> 更新当前项目中的包。
> 更多信息：<https://docs.npmjs.com/cli/commands/npm-update>。

- 更新当前项目中的所有包：

`npm update`

- 更新当前项目中的特定包：

`npm update {{package}}`

- 全局更新一个包：

`npm update -g {{package}}`

- 一次性更新多个包：

`npm update {{package1 package2 ...}}`