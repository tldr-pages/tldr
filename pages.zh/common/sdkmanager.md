# sdkmanager

> 安装 Android SDK 的软件包。
> 更多信息：<https://developer.android.com/tools/sdkmanager>.

- 列出可用的软件包：

`sdkmanager --list`

- 安装一个软件包：

`sdkmanager {{package}}`

- 更新所有已安装的软件包：

`sdkmanager --update`

- 卸载一个软件包：

`sdkmanager --uninstall {{package}}`
