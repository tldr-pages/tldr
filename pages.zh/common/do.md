# do

> 该 shell 关键字用于 `for`、`while`、`select` 和 `until` 中，标记循环的开始。

- 查看 `for` 关键字的文档：

`tldr for`

- 查看 `while` 关键字的文档：

`tldr while`

- 查看 `select` 关键字的文档：

`tldr select`

- 查看 `until` 关键字的文档：

`tldr until`
