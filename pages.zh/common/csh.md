# csh

> 具有 C 语言风格语法的 shell（命令解释器）。
> 参见：`tcsh`。
> 更多信息：<https://www.mkssoftware.com/docs/man1/csh.1.asp>。

- 启动交互式 shell 会话：

`csh`

- 启动不加载启动配置的交互式 shell 会话：

`csh -f`

- 执行特定的 [c]ommands：

`csh -c "{{echo 'csh is executed'}}"`

- 执行特定的脚本：

`csh {{path/to/script.csh}}`