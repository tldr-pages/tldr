# pueue

> 一个用于管理长期运行任务的顺序和并行执行的任务管理工具。
> 某些子命令（如 `add`）有自己的使用文档。
> 更多信息：<https://github.com/Nukesor/pueue>.

- 显示通用帮助和可用的子命令：

`pueue --help`

- 执行 pueue 子命令：

`pueue {{subcommand}}`

- 检查 pueue 的版本：

`pueue --version`
