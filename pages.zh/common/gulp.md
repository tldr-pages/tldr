# gulp

> JavaScript 任务运行器和流式构建系统。
> 任务在项目根目录下的 `gulpfile.js` 文件中定义。
> 更多信息：<https://github.com/gulpjs/gulp-cli>。

- 运行默认任务：

`gulp`

- 运行单个任务：

`gulp {{task}} {{othertask}}`

- 打印已加载 gulpfile 的任务依赖树：

`gulp --tasks`
