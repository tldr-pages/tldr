# cmatrix

> 在终端中显示类似《黑客帝国》电影的滚动字符画面。
> 另请参阅：`neo`、`cbonsai`、`asciiquarium`、`pipes.sh`。
> 更多信息：<https://manned.org/cmatrix>。

- 启用异步滚动：

`cmatrix -a`

- 更改文本颜色（默认为绿色）：

`cmatrix -C {{red}}`

- 启用彩虹模式：

`cmatrix -r`

- 设置画面更新延迟为 100 厘秒（1 秒）：

`cmatrix -u 100`

- 使用屏保模式，按下任意键即退出：

`cmatrix -s`
