# swipl

> SWI-Prolog - 一个全面的免费 Prolog 环境。
> 更多信息：<https://www.swi-prolog.org/>.

- 启动交互式会话：

`swipl`

- 执行命令但不显示任何输出：

`swipl --quiet -t "{{command}}"`

- 执行脚本：

`swipl {{path/to/file.pl}}`

- 打印所有 shell 配置变量：

`swipl --dump-runtime-variables`

- 显示版本：

`swipl --version`