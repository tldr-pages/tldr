# ocaml

> OCaml 的读取-求值-打印循环（REPL）。
> 解释 OCaml 命令。
> 更多信息：<https://ocaml.org>.

- 从用户处读取 OCaml 命令并执行：

`ocaml`

- 从文件中读取 OCaml 命令并执行：

`ocaml {{path/to/file.ml}}`

- 使用模块运行 OCaml 脚本：

`ocaml {{module1}} {{module2}} {{path/to/file.ml}}`