# resume

> 轻松设置新的简历。
> 更多信息：<https://github.com/jsonresume/resume-cli>.

- 在当前工作目录中创建一个新的 `resume.json` 文件：

`resume init`

- 验证 `resume.json` 文件是否符合标准：

`resume validate`

- 以样式化的 HTML 或 PDF 格式本地导出简历：

`resume export {{path/to/html_or_pdf}}`

- 启动一个服务于本地 `resume.json` 的 Web 服务器：

`resume serve`