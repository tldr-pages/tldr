# psysh

> PHP 的运行时开发控制台、交互式调试器和 REPL。
> 更多信息：<https://psysh.org>.

- 在当前目录中打开一个 shell：

`psysh`

- 在指定目录中打开一个 shell：

`psysh --cwd {{path/to/directory}}`

- 使用特定的配置文件：

`psysh --config {{path/to/file}}`
