# pretty-bytes

> 将字节转换为人类可读的字符串。
> 更多信息：<https://github.com/sindresorhus/pretty-bytes-cli>.

- 将数值字节转换为人类可读的字符串：

`pretty-bytes {{1337}}`

- 从 `stdin` 将数值字节转换为人类可读的字符串：

`echo {{1337}} | pretty-bytes`

- 显示帮助：

`pretty-bytes --help`