# git abort

> 中止正在进行的 rebase、merge 或 cherry-pick 操作。
> 属于 `git-extras`。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-abort>.

- 中止 Git 的 rebase、merge 或 cherry-pick 操作：

`git abort`