# browsh

> 使用 Firefox 后端在终端中查看网页。
> 更多信息：<https://www.brow.sh/>.

- 启动 browsh:

`browsh`

- 在特定网页中启动 browsh:

`browsh --startup-url {{URL}}`

- 聚焦 URL 栏:

`<Ctrl l>`

- 退出 browsh:

`<Ctrl q>`

- 显示帮助:

`browsh {{[-h|--help]}}`
