# quota

> 显示用户的磁盘空间使用情况和分配限制。
> 更多信息：<https://manned.org/quota>.

- 以人类可读单位显示当前用户的磁盘限额：

`quota -s`

- 详细输出（也显示没有分配存储的文件系统的限额）：

`quota -v`

- 简洁输出（仅显示使用超过限额的文件系统的限额）：

`quota -q`

- 打印当前用户所属组的限额：

`quota -g`

- 显示其他用户的磁盘限额：

`sudo quota -u {{用户名}}`
