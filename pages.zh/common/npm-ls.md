# npm ls

> 将已安装的包打印到 `stdout`。
> 更多信息：<https://docs.npmjs.com/cli/commands/npm-ls>。

- 将直接依赖的所有版本打印到 `stdout`：

`npm ls`

- 打印所有已安装的包，包括对等依赖：

`npm ls --all`

- 打印带有扩展信息的依赖：

`npm ls --long`

- 以可解析格式打印依赖：

`npm ls --parseable`

- 以 JSON 格式打印依赖：

`npm ls --json`
