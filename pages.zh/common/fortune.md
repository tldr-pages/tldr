# fortune

> 打印一条随机引用（类似幸运饼干的格式）。
> 更多信息：<https://manned.org/fortune>.

- 打印一条引用：

`fortune`

- 打印一条不雅的引用：

`fortune -o`

- 打印一条长引用：

`fortune -l`

- 打印一条短引用：

`fortune -s`

- 列出可用的引用数据库文件：

`fortune -f`

- 从 `fortune -f` 列出的数据库文件之一打印一条引用：

`fortune {{path/to/file}}`