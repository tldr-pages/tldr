# apg

> 生成任意复杂度的随机密码.

- 生成随机密码 (默认密码长度为8位):

`apg`

- 生成密码, 包含至少1个符号 (S), 1个数字 (N), 1 个大写字母(C), 1个小写字母(L):

`apg -M SNCL`

- 生成16个字符的密码:

`apg -m {{16}}`

- 生成最大长度为16位的密码:

`apg -x {{16}}`

- 生成未出现在字典中的密码（必须提供字典文件）

`apg -r {{字典文件}}`
