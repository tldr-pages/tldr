# git coauthor

> 向最近的提交添加另一位作者。由于此命令会重写 Git 历史记录，下次推送时需要使用 `--force`。
> 这是 `git-extras` 的一部分。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-coauthor>.

- 向最后一个 Git 提交添加一位额外的作者：

`git coauthor {{name}} {{name@example.com}}`
