# mariadb-install-db

> 初始化 MariaDB 数据库。
> 更多信息：<https://manned.org/mariadb-install-db>。

- 初始化数据库：

`sudo mariadb-install-db --user {{user}} --basedir {{/usr}} --datadir {{/var/lib/mysql}}`

- 显示帮助：

`mariadb-install-db --help`