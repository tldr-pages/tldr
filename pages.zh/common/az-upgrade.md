# az upgrade

> 升级 Azure CLI 和扩展。
> 作为 `azure-cli`（也称为 `az`）的一部分。
> 更多信息：<https://learn.microsoft.com/cli/azure/reference-index?view=azure-cli-latest#az-upgrade>。

- 升级 Azure CLI:

`az upgrade`

- 升级 Azure CLI 和所有扩展:

`az upgrade --all`

- 升级 Azure CLI 和所有扩展，不提示确认:

`az upgrade --all --yes`