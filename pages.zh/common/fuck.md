# fuck

> 修正你之前的控制台命令。
> 更多信息：<https://github.com/nvbn/thefuck>。

- 将 `fuck` 别名设置为 `thefuck` 工具：

`eval "$(thefuck --alias)"`

- 尝试为上一条命令匹配规则：

`fuck`

- 立即确认第一个选项（取决于烦躁程度，选择的参数不同）：

`fuck --{{yes|yeah|hard}}`