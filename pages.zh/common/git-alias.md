# git alias

> 为 Git 命令创建快捷方式。
> 属于 `git-extras` 部分。
> 更多信息：<https://github.com/tj/git-extras/blob/master/Commands.md#git-alias>.

- 列出所有别名：

`git alias`

- 创建一个新的别名：

`git alias "{{name}}" "{{command}}"`

- 搜索已存在的别名：

`git alias ^{{name}}`