# Set-NodeInstallLocation

> 设置 `ps-nvm` 的默认 Node.js 安装目录。
> 该命令属于 `ps-nvm`，只能在 PowerShell 下运行。
> 更多信息：<https://github.com/aaronpowell/ps-nvm>。

- 将 Node.js 安装位置更改为指定目录（`ps-nvm` 将在其中创建一个新的 `.nvm` 子目录来安装 Node.js）：

`Set-NodeInstallLocation {{path/to/directory}}`