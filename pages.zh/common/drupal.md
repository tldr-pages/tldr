# drupal

> 生成样板代码，与 Drupal 项目交互并进行调试。
> 一些子命令（如 `check`）有自己的使用文档。
> 更多信息：<https://drupalconsole.com/>.

- 安装模块：

`drupal module:install {{module_name}}`

- 卸载模块：

`drupal module:uninstall {{module_name}}`

- 清除所有缓存：

`drupal cache:rebuild`

- 查看当前 Drupal 安装状态：

`drupal site:status`
