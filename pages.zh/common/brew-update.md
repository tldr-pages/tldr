# brew update

> 获取 Homebrew 和所有公式的最新版本，使用 `git` 从 GitHub 拉取并执行必要的迁移。
> 要升级所有已安装的公式，请使用 `brew upgrade`。
> 更多信息：<https://docs.brew.sh/Manpage#update-up-options>.

- 获取 Homebrew 和所有公式的最新版本：

`brew update`