# grip

> 预览本地的 GitHub 风格 Markdown 文件。
> 更多信息：<https://github.com/joeyespo/grip>.

- 启动服务器并提供当前目录下的 `README` 文件的渲染版本：

`grip`

- 启动服务器并提供特定的 Markdown 文件：

`grip {{path/to/file.md}}`

- 启动服务器并在浏览器中打开当前目录下的 `README` 文件：

`grip --browser`

- 在指定端口启动服务器并提供当前目录下的 `README` 文件的渲染版本：

`grip {{port}}`
