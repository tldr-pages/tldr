# pinky

> 使用 `finger` 协议打印用户信息。
> 更多信息：<https://manned.org/pinky>。

- 显示当前用户的详细信息：

`pinky`

- 显示特定用户的详细信息：

`pinky {{用户}}`

- 以长格式显示详细信息：

`pinky {{用户}} -l`

- 在长格式中省略用户的主目录和 shell：

`pinky {{用户}} -lb`

- 在长格式中省略用户的项目文件：

`pinky {{用户}} -lh`

- 在短格式中省略列标题：

`pinky {{用户}} -f`
