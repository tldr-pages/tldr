# wget

- 非交互式的网络文件下载工具
  
`wget [选项]... [URL]...`

- 查看原始wget命令的文档：

`tldr wget {{[-p|--platform]}} common`

- 查看PowerShell的Invoke-WebRequest命令的文档：

`tldr invoke-webrequest`

- 显示版本:

`wget --version`
