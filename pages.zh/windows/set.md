# set

> 显示或设置当前命令行实例的环境变量。
> 更多信息：<https://learn.microsoft.com/windows-server/administration/windows-commands/set>.

- 列出当前所有环境变量：

`set`

- 为一个环境变量设置指定的值：

`set {{变量名}}={{值}}`

- 列出以指定字符串开头的环境变量：

`set {{变量名}}`

- 提示用户输入指定变量的值：

`set /p {{变量名}}={{提示信息}}`
