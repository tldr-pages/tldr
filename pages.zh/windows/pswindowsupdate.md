# PSWindowsUpdate

> 用于管理 Windows 更新的 PowerShell 外部模块。
> 该工具提供多个命令，但所有命令都只能通过 PowerShell 运行。
> 更多信息：<https://github.com/mgajda83/PSWindowsUpdate>.

- 使用 `Install-Module` 安装模块：

`Install-Module PSWindowsUpdate`

- 列出模块下的所有可用命令：

`Get-Command -Module PSWindowsUpdate`
