# ipconfig

> 显示和管理Windows的网络配置.

- 显示网络适配器列表:

`ipconfig`

- 显示网络适配器的详细列表:

`ipconfig /all`

- 为一个网络适配器重新获取IP地址:

`ipconfig /renew {{adapter}}`

- 为一个网络适配器释放IP地址:

`ipconfig /release {{adapter}}`

- 清除所有DNS缓存:

`ipconfig /flushdns`
