# docker buildx stop

> Останавливать экземпляр сборщика.
> Больше информации: <https://docs.docker.com/reference/cli/docker/buildx/stop>.

- Остановить текущий экземпляр сборщика:

`docker buildx stop`

- Остановить указанный экземпляр сборщика:

`docker buildx stop {{имя_сборщика}}`
