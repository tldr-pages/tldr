# git abort

> Прерывать текущее перебазирование (rebase), слияние (merge) или cherry-pick.
> Часть `git-extras`.
> Больше информации: <https://github.com/tj/git-extras/blob/main/Commands.md#git-abort>.

- Прервать перебазирование (rebase), слияние (merge) или cherry-pick в Git:

`git abort`
