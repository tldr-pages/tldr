# systemd-confext

> Cette commande est un alias de `systemd-sysext`.
> Elle fonctionne selon le même principe que `systemd-sysext`, mais au lieu d'agir sur `/usr` et `/opt`, `confext` étend uniquement `/etc`.

- Affiche la documentation de la commande originale :

`tldr systemd-sysext`
