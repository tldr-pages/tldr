# yum

> Cette commande est un alias de `dnf` sur les distributions modernes.
> Sur les anciennes distributions (par exemple, CentOS 7), `yum` est un programme distinct de `dnf`. Certaines sous-commandes et options peuvent être différentes.

- Affiche la documentation de la commande originale :

`tldr dnf`
