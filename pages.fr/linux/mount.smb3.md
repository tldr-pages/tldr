# mount.smb3

> Cette commande est un alias de `mount.cifs`.
> Remarque : Pour les versions de SMB antérieures à 3, utiliser `mount.cifs` à la place.

- Affiche la documentation de la commande originale :

`tldr mount.cifs`
