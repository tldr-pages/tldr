# lima

> Cette commande est un alias de `limactl shell` pour l’instance de machine virtuelle par défaut.
> Vous pouvez également définir la variable d’environnement `$LIMA_INSTANCE` pour travailler sur une autre instance.

- Affiche la documentation de la commande originale :

`tldr limactl`
