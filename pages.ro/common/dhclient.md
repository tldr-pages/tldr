# dhclient

> client DHCP.
> Mai multe informaţii: <https://manned.org/dhclient>

- Obțineți o adresă IP pentru interfața `eth0`:

`sudo dhclient {{eth0}}`

- Eliberați o adresă IP pentru interfața `eth0`:

`sudo dhclient -r {{eth0}}`
