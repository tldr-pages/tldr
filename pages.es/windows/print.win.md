# print

> Imprime un archivo de texto en una impresora.
> Más información: <https://learn.microsoft.com/windows-server/administration/windows-commands/print>.

- Imprime un archivo de texto en la impresora predeterminada:

`print {{ruta\al\archivo}}`

- Imprime un archivo de texto en una impresora específica:

`print /d:{{impresora}} {{ruta\al\archivo}}`
