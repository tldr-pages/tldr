# rd

> Este comando es un alias de `rmdir` en la consola de comandos y subsecuentemente, de `Remove-Item` en PowerShell.

- Vea la documentación del comando original de la consola:

`tldr rmdir`

- Vea la documentación del comando original de PowerShell:

`tldr remove-item`
