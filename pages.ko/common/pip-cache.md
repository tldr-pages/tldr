# pip cache

> pip wheel 캐시 검사 및 관리.
> 더 많은 정보: <https://pip.pypa.io/en/stable/cli/pip_cache/>.

- pip cache 디렉터리 위치 표시:

`pip cache dir`

- 현재 캐시에 저장된 모든 패키지 파일 이름 목록 표시:

`pip cache list`

- pip 캐시의 모든 파일 제거:

`pip cache purge`

- 특정 패키지 이름과 일치하는 캐시 파일 제거:

`pip cache remove {{패키지_이름}}`
