# nix-channel

> `nix` 업데이트 채널 관리.
> 더 많은 정보: <https://nixos.wiki/wiki/Nix_channels>.

- 현재 채널 목록 표시:

`nix-channel --list`

- 채널 추가:

`nix-channel --add {{https://nixos.org/channels/nixpkgs-unstable}}`

- 모든 채널의 패키지 목록 업데이트:

`nix-channel --update`
