# terraform logout

> 원격 호스트에서 로그아웃.
> 관련 항목: `terraform login`.
> 더 많은 정보: <https://developer.hashicorp.com/terraform/cli/commands/logout>.

- HCP Terraform (app.terraform.io)에서 로그아웃:

`terraform logout`

- 지정한 호스트로부터 로그아웃:

`terraform logout {{호스트}}`
