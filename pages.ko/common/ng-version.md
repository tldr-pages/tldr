# ng version

> Angular CLI 버전 정보 표시.
> 더 많은 정보: <https://angular.dev/cli/version>.

- 버전 정보 표시:

`ng {{[v|version]}}`

- CLI, framework, 시스템 및 프로젝트 의존성 버전을 JSON 형식으로 출력:

`ng {{[v|version]}} --json`
