# git release

> 릴리스를 위한 Git 태그 생성.
> `git-extras`의 일부.
> 더 많은 정보: <https://manned.org/git-release>.

- 릴리스 생성 및 푸시:

`git release {{태그_이름}}`

- 서명된 릴리스 생성 및 푸시:

`git release {{태그_이름}} -s`

- 메시지와 함께 릴리스 생성 및 푸시:

`git release {{태그_이름}} -m "{{메시지}}"`
