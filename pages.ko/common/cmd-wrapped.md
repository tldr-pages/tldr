# cmd-wrapped

> 쉘 명령 기록 통계를 표시.
> 더 많은 정보: <https://crates.io/crates/cmd-wrapped>.

- 현재 통계를 조회:

`cmd-wrapped`

- 특정 연도의 통계를 조회:

`cmd-wrapped {{연도}}`

- 사용한 쉘을 지정:

`cmd-wrapped {{[-s|--shell]}} {{쉘}}`

- 도움말 표시:

`cmd-wrapped {{[-h|--help]}}`

- 버전 표시:

`cmd-wrapped {{[-V|--version]}}`
