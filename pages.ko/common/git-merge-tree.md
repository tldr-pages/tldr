# git merge-tree

> 파일을 변경하지 않고 브랜치 병합을 수행.
> 더 많은 정보: <https://git-scm.com/docs/git-merge-tree>.

- 두 브랜치 간 병합의 결과를 표시:

`git merge-tree {{브랜치1}} {{브랜치2}}`

- 병합을 수행하고 결과 트리를 작성:

`git merge-tree --write-tree {{브랜치1}} {{브랜치2}}`
