# tailscale set

> 지정한 tailscale 설정을 변경.
> 이러한 옵션은 초기 설정 과정에서도 변경 가능.
> `tailscale up` 문서에 설명된 모든 옵션을 이 명령으로 설정 가능.
> 더 많은 정보: <https://tailscale.com/kb/1080/cli#set>.

- `tailscale up` 문서 보기:

`tldr tailscale up`
