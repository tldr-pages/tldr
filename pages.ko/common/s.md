# s

> 터미널에서 웹 검색.
> 더 많은 정보: <https://github.com/zquestz/s>.

- Google(기본 제공자)에서 쿼리 검색:

`s {{쿼리}}`

- 모든 제공자 나열:

`s --list-providers`

- 지정된 제공자로 쿼리 검색:

`s --provider {{제공자}} {{쿼리}}`

- 지정된 바이너리를 사용하여 검색 쿼리 수행:

`s --binary "{{바이너리}} {{인수}}" {{쿼리}}`
