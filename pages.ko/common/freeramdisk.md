# freeramdisk

> 레거시 시스템에서 `loadlin` 램디스크가 사용 중인 메모리 해제.
> 현재는 `umount`, `losetup`, `tmpfs`로 대체되어 사용이 권장되지 않음.
> 더 많은 정보: <https://manned.org/freeramdisk>.

- Free `loadlin` 램디스크 메모리 해제:

`sudo freeramdisk`
