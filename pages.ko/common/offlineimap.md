# offlineimap

> 원격 IMAP 서버를 로컬 Maildir 폴더와 동기화.
> 더 많은 정보: <https://www.offlineimap.org>.

- 자동 새로 고침 없이 한 번 동기화:

`offlineimap -o`

- 특정 계정 동기화:

`offlineimap -a {{계정}}`

- 특정 폴더 동기화:

`offlineimap -f {{폴더}}`
