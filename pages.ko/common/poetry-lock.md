# poetry lock

> pyproject.toml`에 정의된 의존성의 버전 잠금 (의존성 설치하지 않음).
> 관련 항목: `asdf`.
> 더 많은 정보: <https://python-poetry.org/docs/cli/#lock>.

- 현재 디렉터리의 `pyproject.toml` 파일에 정의된 의존성 잠금:

`poetry lock`

- 기존 잠금 파일을 새로 생성:

`poetry lock --regenerate`
