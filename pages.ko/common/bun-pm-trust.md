# bun pm trust

> 의존성을 신뢰된 상태로 표시.
> 관련 항목: `bun pm untrusted`.
> 더 많은 정보: <https://bun.com/docs/pm/cli/pm#trust>.

- 특정 의존성을 신뢰로 설정하고 `trustedDependencies`에 추가:

`bun pm trust {{의존성_이름}}`

- 현재 신뢰되지 않은 모든 의존성을 신뢰로 설정:

`bun pm trust --all`
