# ng completion

> Angular CLI용 쉘 자동완성 스크립트 생성.
> 더 많은 정보: <https://angular.dev/cli/completion>.

- 현재 쉘에 대해 대화형으로 자동완성 설정:

`ng completion`

- `bash` 또는 `zsh` 용 자동완성 스크립트 생성:

`ng completion script`

- 현재 쉘 세션에 자동완성 스크립트 적용:

`source <(ng completion script)`
