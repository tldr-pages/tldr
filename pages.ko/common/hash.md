# hash

> 캐시된 실행 파일 위치 조회.
> 더 많은 정보: <https://www.gnu.org/software/bash/manual/bash.html#index-hash>.

- 현재 쉘의 캐시된 명령어 위치 표시:

`hash`

- 해시 테이블 초기화:

`hash -r`

- 해시 테이블에서 특정 명령 제거:

`hash -d {{명령어}}`

- 명령어의 전체 경로 출력:

`hash -t {{명령어}}`

- 도움말 표시:

`hash --help`
