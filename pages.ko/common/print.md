# print

> `print`는 동일한 이름을 가진 여러 명령을 가리킬 수 있음.

- Zsh 내장 print 명령 문서 보기:

`tldr print.zsh`

- `run-mailcap --action=print` 별칭의 문서 보기:

`tldr {{[-p|--platform]}} linux run-mailcap`

- Windows의 텍스트 파일 출력 명령 문서 보기:

`tldr {{[-p|--platform]}} windows print.win`
