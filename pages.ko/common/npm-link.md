# npm link

> 로컬 패키지를 전역 `node_modules` 또는 다른 프로젝트에서 심볼릭 링크로 연결하는 명령어.
> 더 많은 정보: <https://docs.npmjs.com/cli/npm-link/>.

- 현재 패키지를 전역으로 링크:

`npm {{[ln|link]}}`

- 전역에 링크된 패키지를 다른 프로젝트의 `node_modules`에 연결:

`npm {{[ln|link]}} {{패키지_이름}}`

- 현재 프로젝트에서 패키지 링크 해제:

`npm {{[r|unlink]}} {{패키지_이름}}`
