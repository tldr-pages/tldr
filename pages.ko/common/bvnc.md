# bvnc

> 로컬 네트워크에서 SSH/VNC 서버를 검색하기 위한 GUI 도구.
> 참고: `bssh` 및 `bshell`.
> 더 많은 정보: <https://manned.org/bvnc>.

- VNC 서버 탐색:

`bvnc`

- SSH 서버 탐색:

`bvnc --ssh`

- VNC 및 SSH 서버 탐색:

`bvnc --shell`

- 지정된 도메인에서 VNC 서버를 찾음:

`bvnc --domain {{도메인}}`
