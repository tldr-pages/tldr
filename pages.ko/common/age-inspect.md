# age-inspect

> `age`로 암호화된 파일 검사.
> 관련 항목: `age`, `age-keygen`.
> 더 많은 정보: <https://manned.org/age-inspect>.

- 수신자 유형, 파일 크기, 포스트 양자 암호화 사용 여부 출력:

`age-inspect {{경로/대상/파일}}`

- 출력을 JSON 형식으로 `stdout`에 출력:

`age-inspect --json {{경로/대상/파일}}`
