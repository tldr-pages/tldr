# git show-ref

> Git 레퍼런스를 나열하는 명령어.
> 더 많은 정보: <https://git-scm.com/docs/git-show-ref>.

- 저장소의 모든 레퍼런스 표시:

`git show-ref`

- 헤드 레퍼런스만 표시:

`git show-ref --heads`

- 태그 레퍼런스만 표시:

`git show-ref --tags`

- 주어진 레퍼런스가 존재하는지 확인:

`git show-ref --verify {{경로/대상/레퍼런스}}`
