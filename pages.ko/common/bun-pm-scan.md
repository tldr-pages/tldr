# bun pm scan

> Bun에 내장된 보안 스캐너를 사용하여 패키지의 보안 취약점을 검사.
> 이 검사는 `bunfig.toml`에서 스캐너를 설정하고 npm에서 `@bun-security-scanner/osv`를 설치한 이후, `bun install` 실행 시 자동으로 수행됨.
> 더 많은 정보: <https://bun.com/docs/pm/security-scanner-api>.

- 의존성의 보안 취약점 검사:

`bun pm scan`
