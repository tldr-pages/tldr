# npm install-ci-test

> 프로젝트 의존성을 설치하고 테스트를 실행하는 명령어로, 주로 CI(지속적 통합) 환경에서 사용됨.
> `npm ci && npm test` 실행과 동일한 동작.
> 더 많은 정보: <https://docs.npmjs.com/cli/install-ci-test/>.

- 의존성 설치 후 테스트 실행:

`npm {{[cit|install-ci-test]}}`

- 설치 및 테스트 과정의 상세 로그 출력:

`npm {{[cit|install-ci-test]}} --verbose`
