# convert

> 이 명령은 `magick convert`의 별칭입니다.
> 참고: ImageMagick 7부터 이 별칭은 사용되지 않음. `magick`으로 대체됨.
> 7 이상 버전에서 이전 도구를 사용해야 하는 경우, `magick convert`를 사용해야 함.

- 자세한 내용은 원본 명령을 참고하세요:

`tldr magick convert`
