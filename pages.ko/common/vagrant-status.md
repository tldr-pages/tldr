# vagrant status

> 현재 Vagrant 환경에 있는 machine의 상태를 표시.
> 관련 항목: `vagrant`, `vagrant global-status`.
> 더 많은 정보: <https://developer.hashicorp.com/vagrant/docs/cli/status>.

- 현재 디렉터리에 있는 machine의 상태 표시:

`vagrant status`

- 이름 또는 ID를 지정하여 machine 상태 표시 :

`vagrant status {{이름|id}}`
