# elasticsearch-syskeygen

> Elasticsearch 설정 디렉터리에 시스템 키 파일을 생성하는 도구.
> 더 많은 정보: <https://www.elastic.co/docs/reference/elasticsearch/command-line-tools/syskeygen>.

- 기본 `$ES_HOME/config` 디렉터리에 `system_key` 파일 생성:

`elasticsearch-syskeygen`
