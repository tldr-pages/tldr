# npm ping

> `npm` 레지스트리에 ping을 전송하여 연결 가능한지 확인하는 명령어.
> 더 많은 정보: <https://docs.npmjs.com/cli/npm-ping/>.

- 기본 `npm` 레지스트리 ping 전송:

`npm ping`

- 사용자 정의 `npm` 레지스트리 ping 전송:

`npm ping --registry {{사용자정의_레지스트리_주소}}`
