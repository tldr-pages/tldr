# mpstat

> CPU 사용 정보를 표시합니다.
> 더 많은 정보: <https://manned.org/mpstat>.

- 2초마다 CPU 통계를 표시:

`mpstat {{2}}`

- 2초 간격으로 하나씩 5개의 보고서 표시:

`mpstat {{2}} {{5}}`

- 특정 프로세서에서 2초 간격으로 하나씩 5개의 보고서 표시:

`mpstat -P {{0}} {{2}} {{5}}`
