# impala

> TUI를 통해 Wi-Fi 네트워크를 관리.
> 더 많은 정보: <https://github.com/pythops/impala#-usage>.

- station 모드로 `impala` 실행:

`impala`

- Access Point 모드로 `impala` 실행:

`impala {{[-m|--mode]}} ap`

- 섹션 간 전환:

`{{<Tab>|<Shift Tab>}}`

- 연결할 네트워크 선택:

`<Space>`

- 단축키 표시:

`<?>`
