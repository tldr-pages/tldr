# cec-ctl

> 커널 CEC 장치 제어.
> 관련 항목: `cec-client`.
> 더 많은 정보: <https://manned.org/cec-ctl>.

- CEC 장치 목록 표시:

`cec-ctl --list-devices`

- CEC 트래픽 모니터링:

`sudo cec-ctl {{[-m|--monitor]}}`

- CEC 토폴로지 표시:

`cec-ctl {{[-S|--show-topology]}}`
