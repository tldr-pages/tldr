# fancontrol

> 팬 속도를 자동으로 관리.
> 설정 파일을 사용하여 다양한 변수를 제어.
> 더 많은 정보: <https://manned.org/fancontrol>.

- 기본 `/etc/fancontrol` 설정 파일을 사용하여 `fancontrol` 시작:

`fancontrol`

- 사용자 지정 설정 파일을 사용하여 `fancontrol` 시작:

`fancontrol {{경로/대상/설정_파일}}`
