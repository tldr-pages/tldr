# look

> Look for lines in sorted file.
> More information: <https://ss64.com/osx/look.html>.

- Look for lines which begins with the given prefix:

`look {{prefix}} {{file}}`

- Look for lines ignoring case:

`look -f {{prefix}} {{file}}`
