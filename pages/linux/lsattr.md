# lsattr 

> List file attributes on a Linux file system

- list file attributes recursively in current directory

`lsattr -R `

- list attributes of all files in directory, including hidden ones

`lsattr -a`

- list attributes of directories

`lsattr -d`
