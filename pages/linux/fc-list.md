# fc-list

> List available fonts installed on the system.

- Returns a list of installed fonts in your system:

`fc-list`

- Returns a list of installed fonts with given name:

`fc-list | grep '{{DejaVu Serif}}'`

- Returns the number of installed fonts in your system:

`fc-list | wc -l`
