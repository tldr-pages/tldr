#dpkg

> debian package manager

- install a package

`dpkg -i {{/path/to/file}}`

- remove a package

`dpkg -r {{package_name}}`

- list installed packages

`dpkg -l {{pattertn}}`

- list package contents

`dpkg -L {{package_name}}`
