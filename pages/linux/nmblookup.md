# nmblookup

> Discover SMB shares.

- Find hosts in the local network with SMB shares:

`nmblookup -S '**'`

- Find hosts in the local network with SMB shares ran by SAMBA:

`nmblookup -S __SAMBA__`
