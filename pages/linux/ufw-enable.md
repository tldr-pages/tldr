# ufw enable

> Reload and enables firewall on boot.
> More information: <https://manned.org/ufw>.

- Mock reload and enable `ufw`:

`sudo --dry-run enable`

- Reload and enable `ufw`:

`sudo ufw enable`
