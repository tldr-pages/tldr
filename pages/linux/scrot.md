# scrot

> Capture screenshots using the command line.

- Capture the whole screen:

`scrot`

- Select a window with the mouse then capture the window:

`scrot -s`

- Wait 5 seconds before taking a screenshot and display a countdown:

`scrot -d 5 -c`
