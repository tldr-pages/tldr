# fancontrol

> Automated fan speed management.
> Uses a config file to control a wide range of variables.
> More information: <https://manned.org/fancontrol>.

- Start `fancontrol`:

`fancontrol {{path/to/configfile}}'
