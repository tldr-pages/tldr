# ascii-xfr

> Transfer files using the ASCII protocol.
> Part of `minicom`.
> More information: <https://manned.org/man/ascii-xfr>.

- Send a file:

`ascii-xfr -s {{file}}`

- Receive a file:

`ascii-xfr -r {{file}}`
