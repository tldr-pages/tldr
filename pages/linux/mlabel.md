# mlabel

> Set an MS-DOS volume label for fat and vfat filesystems.
> More information: <https://www.gnu.org/software/mtools/>.

- Set a filesystem label:

`mlabel -i /dev/{{sda}} ::"{{new_label}}"`
