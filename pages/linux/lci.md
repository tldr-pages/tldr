# lci

> LOLCODE interpreter written in C.
> More information: <http://www.lolcode.org>.

- Run a file written in LOLCODE:

`lci {{path/to/file}}`

- Display help:

`lci -h`

- Display version:

`lci -v`
