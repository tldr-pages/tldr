# terraform stacks version

> Display the version of the Stacks plugin.
> More information: <https://developer.hashicorp.com/terraform/cli/commands/stacks/version>.

- Display the version in human-readable format:

`terraform stacks version`

- Display the version in JSON format:

`terraform stacks version -json`
