# adb shell pm install-existing

> Re-add a pre-existing app to a given user.
> More information: <https://developer.android.com/tools/adb>.

- Re-add an app:

`adb shell pm install-existing {{app_name}}`
