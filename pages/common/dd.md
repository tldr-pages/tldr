# dd

> "Data Duplicator", command often used for creating disk images,
> backing up partitions, converting data, or just creating files.

- Burning an iso to a disk or drive

`dd if={{ubuntu.iso}} of={{unmounted drive}}`

- Cloning an external device 

`dd if={{input_drive}} of={{output_drive}}`
