# terraform stacks init

> Initialize a Stack's component configuration by downloading configuration dependencies.
> More information: <https://developer.hashicorp.com/terraform/cli/commands/stacks/init>.

- Initialize a Stack's component configuration:

`terraform stacks init`

- Initialize and upgrade to the latest allowed module and provider versions:

`terraform stacks init -upgrade`
