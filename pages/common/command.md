# command

> Command forces the shell to execute the program and ignore any functions, builtins and aliases with the same name.

- Causes shell to execute the ls program, even if an ls alias exists:

`command {{ls}}`
