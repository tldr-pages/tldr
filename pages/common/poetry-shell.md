# poetry shell

> Spawn a shell within the Poetry virtual environment.
> More information: <https://python-poetry.org/docs/cli/#shell>.

- Spawn a new shell within the project's virtual environment:

`poetry shell`

- Exit the spawned shell and deactivate the virtual environment:

`exit`
