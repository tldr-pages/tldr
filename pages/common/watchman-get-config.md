# watchman get-config

> Show the configuration for a watched directory.
> More information: <https://facebook.github.io/watchman/>.

- Get config of a directory:

`watchman get-config {{path/to/watched_directory}}`
