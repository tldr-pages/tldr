# kubectx

> Utility which is able to manage and switch between kubectl contexts.

- List the contexts:

`kubectx`

- Switch to context:

`kubectx {{name}}`

- Switch to previous context:

`kubectx -`

- Delete context:

`kubectx -d {{name}}`
