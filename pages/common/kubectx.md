# kubectx

> Utility to manage and switch between `kubectl` contexts.

- List the contexts:

`kubectx`

- Switch to context:

`kubectx {{name}}`

- Switch to the previous context:

`kubectx -`

- Delete context:

`kubectx -d {{name}}`
