# vagrant status

> Display the state of the machine/s in the current Vagrant environment.
> See also: `vagrant`.
> More information: <https://developer.hashicorp.com/vagrant/docs/cli/status>.

- View status of the machine/s in the current directory:

`vagrant status`

- Target a machine by name or ID:

`vagrant status {{name|id}}`
