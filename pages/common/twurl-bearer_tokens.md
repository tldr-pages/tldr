# twurl bearer_tokens

> List consumer keys that have an issued application-only (Bearer) token.
> More information: <https://github.com/twitter/twurl#usage>.

- List consumer keys with an issued Bearer token:

`twurl bearer_tokens`
