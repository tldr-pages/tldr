# jj git remote list

> List Git remotes.
> See also: `jj git remote add`, `jj git remote remove`, `jj git remote rename`, `jj git remote set-url`.
> More information: <https://docs.jj-vcs.dev/latest/cli-reference/#jj-git-remote-list>.

- List all Git remotes and their URLs:

`jj git remote list`
