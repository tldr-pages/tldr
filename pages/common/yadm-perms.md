# yadm-perms

> Update permissions as described in the PERMISSIONS section.
> It is usually unnecessary to run this command, as yadm  automatically  processes permissions by default.
> This automatic behavior can be disabled by.
> Setting the configuration yadm.auto-perms to "false".

- To execute:

`yadm perms`
