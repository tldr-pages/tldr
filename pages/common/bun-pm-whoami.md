# bun whoami

> Display the npm username associated with the current credentials.
> Requires being logged in (`bunx npm login`) with credentials in either `bunfig.toml` or `.npmrc`.
> More information: <https://bun.com/docs/pm/cli/pm#whoami>.

- Display the current `npm` username:

`bun pm whoami`
