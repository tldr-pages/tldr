# minecraft

> Run a headless Minecraft server.
> More information: <https://www.minecraft.net/en-us/download/server>.

- Start a minecraft server and generate it if it doesn't exist:

`java -jar {{path/to/server.jar}} nogui`

- Start a server with a GUI:

`java -jar {{path/to/server.jar}}`
