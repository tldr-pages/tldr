# source

> Execute commands from a file in the current shell.

- Cause bash to re-read its initialization file:

`source ~/.bashrc`

- Cause bash to re-read its aliases:

`source ~/.bash_aliases`

- Cause fish to re-read its initialization file:

`source ~/.config/fish/config.fish`
