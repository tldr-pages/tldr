# docsify

> Initialize and serve markdown documentation.
> More information: <https://cli.docsifyjs.org>.

- Initialize new documentation in the current directory:

`docsify init`

- Generate new documentation in the specified directory:

`docsify init {{path}}`

- Serve local documentation on localhost:3000 with live reload:

`docsify serve {{path}}`
