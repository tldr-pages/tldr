# diffimg

> Calculates the intersection (difference) between two images.
> Generates an image where each pixel is the difference between corresponding pixels in the source images.
> More information: <https://graphviz.org/pdf/diffimg.1.pdf>.

- Compare two images and output the difference image to `out.png`:

`diffimg {{image1}} {{image2}} {{outimage}}`
