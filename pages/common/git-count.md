# git count

> Print a commit count.
> Part of `git-extras`.
> More information: <https://github.com/tj/git-extras/blob/master/Commands.md#git-count>.

- Print a total commit count:

`git count`

- Print a total commit count and individuals commit count:

`git count --all`
