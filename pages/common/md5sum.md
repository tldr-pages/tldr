# md5sum

> Print or check an MD5 checksum.

- Print a checksum:

`md5sum {{file}}`

- Print a checksum to a file:

`md5sum {{file}} > {{checksum}}`

- Check a checksum:

`md5sum --check {{checksum}}`
