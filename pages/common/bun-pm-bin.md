# bun pm bin

> Print the path to Bun's package manager bin directory.
> More information: <https://bun.com/docs/pm/cli/pm>.

- Print the local project's `node_modules/.bin` directory:

`bun pm bin`

- Print the global Bun bin directory:

`bun pm bin -g`
