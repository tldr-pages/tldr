# bun pm bin

> Display the path to local or global `bin` directories.
> More information: <https://bun.com/docs/pm/cli/pm#bin>.

- Get the path to the local project's `bin` directory:

`bun pm bin`

- Get the path to the global `bin` directory:

`bun pm bin {{[-g|--global]}}`
