# git

> Distributed version control system.

- Check the Git version:

`git --version`

- Call general help:

`git --help`

- Call help on a command:

`git help {{command}}`

- Call tldr on a command:

`tldr {{git-command}}`

- Execute Git command:

`git {{command}}`
