# arp

> Displays and modifies the Internet-to-Ethernet address translation
> tables used by the address resolution protocol.

- show all arp entries [as numeric addresses]

`arp -a [-n]`

- clear arp cache

`sudo arp -a -d`

