# wc

> Count words, bytes, or lines.

- Count lines in file:

`wc -l {{file}}`

- Count bytes in file:

`wc -c {{file}}`

- Count characters in file (taking multi-byte character sets into account):

`wc -m {{file}}`
