# watchman watch-project

> Watch a directory and its subdirectories by finding the project root.
> More efficient than watching nested directories separately.
> More information: <https://facebook.github.io/watchman/>.

- Watch a project directory and all subdirectories:

`watchman watch-project {{path/to/project_directory}}`
