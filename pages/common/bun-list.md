# bun list

> This command is an alias of `bun pm ls`.

- View documentation for the alias command:

`bun pm ls`

- List dependencies in the current project:

`bun list`

- List all transitive dependencies:

`bun list {{[-a|--all]}}`
