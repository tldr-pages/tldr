# uniq

> Report or omit repeated lines.

- Display each line once.

`uniq {{file}}`

- Display occurence of each line along with that line.

`uniq -c {{file}}`

- Display each duplicate line once.

`uniq -d {{file}}`

- Display unique lines.

`uniq -u {{file}}`
