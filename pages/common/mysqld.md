# mysqld

> The MySQL Server.

- Display error log messages:

`mysqld --console`

- Log connections and queries to a file:

`mysqld --log={{file}}`

- Log errors and start messages a file:

`mysqld --log-error={{file}}`

- Set the port number to use when listening for connections:
`mysqld --port={{number}}`
