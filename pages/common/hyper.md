# hyper

> A terminal built on web technologies.
> More information: <https://hyper.is>.

- Search plugins:

`hyper search {{plugin}}`

- Install plugin:

`hyper install {{plugin}}`

- Remove plugin:

`hyper remove {{plugin}}`

- List installed plugins:

`hyper list`

- List available plugins:

`hyper list-remote`
