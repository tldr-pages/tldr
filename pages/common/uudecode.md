# uudecode

> Decode files encoded by `uuencode`.
> More information: <https://manned.org/uudecode>.

- Decode a file that was create with `uuencode` into the current directory:

`uudecode {{path/to/file}}`
