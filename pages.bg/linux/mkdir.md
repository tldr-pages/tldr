# mkdir

> Създава директории.
> Повече информация: <https://manned.org/mkdir>.

- Създаване на директория:

`mkdir {{directory}}`

- Създаване на родителски директории при нужда:

`mkdir -p {{path/to/directory}}`

- Показване на помощ:

`mkdir --help`

- Показване на версията:

`mkdir --version`
