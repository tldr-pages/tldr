# git update-index

> インデックスを操作するGitコマンド。
> 詳細情報: <https://git-scm.com/docs/git-update-index>。

- 変更されたファイルを未変更のように扱う (`git status` には変更として表示されない):

`git update-index --skip-worktree {{ディレクトリ/サブディレクトリ/変更済みファイル}}`
