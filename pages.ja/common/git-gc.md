# git gc

> 不要なファイルを削除してローカルリポジトリを最適化する。
> 詳細情報: <https://git-scm.com/docs/git-gc>。

- リポジトリを最適化する:

`git gc`

- より積極的に最適化する (時間がかかる):

`git gc --aggressive`

- looseオブジェクトを削除しない (既定では削除する):

`git gc --no-prune`

- すべての出力を抑制する:

`git gc --quiet`

- ヘルプを表示する:

`git gc --help`
