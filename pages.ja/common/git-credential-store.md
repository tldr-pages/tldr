# git credential-store

> パスワードをディスクへ保存するGit認証情報ヘルパー。
> 詳細情報: <https://git-scm.com/docs/git-credential-store>。

- Gitの認証情報を指定したファイルに保存する:

`git config credential.helper 'store --file={{ディレクトリ/サブディレクトリ/ファイル}}'`
