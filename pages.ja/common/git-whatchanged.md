# git whatchanged

> このコマンドは `git log --raw --no-merges` (各コミットの生の差分を含むログ) の別名。
> 注: このコマンドは非推奨であり、将来削除される予定。
> 詳細情報: <https://git-scm.com/docs/git-whatchanged>。

- 元のコマンドのドキュメントを表示する:

`tldr git log`
